
# Streaming camera over UDP with ffmpeg in low latency
########

# Publish
ffmpeg -i /dev/video0 -tune zero_latency -f mpegts udp://<address>:8080

# Receive
ffplay -fflags nobuffer -flags low_delay -f mpegts udp://<address>:8080
